# `spmd_types.rules`

Type-only operations for composing `spmd_typecheck` hooks.  See
[Composing typing rules](../rules.md) for the guide.

```{eval-rst}
.. automodule:: spmd_types.rules
   :members: einsum, all_reduce, all_gather, reduce_scatter, all_to_all, reinterpret, convert, ignore, output, NdimWithSpmdType
```
